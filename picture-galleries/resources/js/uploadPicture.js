document.getElementById("uploadPictureForm").addEventListener("submit", function (event) {
    event.preventDefault()
    var formData = new FormData(uploadPictureForm)
    console.log(formData)

    //we want to send it to amazon

    //1. find url

    //1.5 get all data

    //2. send data

});
